# SSH port (if changed from default 22)
ssh_port: 22

# Allow sFTP
sftp_enabled: false

# List of allowed SSH users
ssh_allowed_users: []

# ClientAliveInterval (in seconds)
client_alive_interval: 120

# MaxAuthTries (number of authentication attempts)
max_auth_tries: 3