storage "file" {
  path = "{{ vault_data_path }}"
}
 
listener "tcp" {
  address     = "0.0.0.0:{{ vault_port }}"
  tls_disable = 1
}
 
api_addr = "http://{{ vault_hostname }}:{{ vault_port }}"
ui = {{ vault_ui }}
